name: 'hw1'
version: '1.0.0'
config-version: 2

profile: 'hw1'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
analysis-paths: ["analyses"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  hw1:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table